<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login | Notes App</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-zinc-950 text-white flex flex-col min-h-screen">
    <!-- Navbar -->
    <nav
      class="h-16 flex justify-between items-center lg:px-10 px-3 border-b border-gray-800 bg-zinc-900/60"
    >
      <a class="lg:text-2xl text-lg font-bold cursor-pointer" href="/">📝 NotesApp</a>

      <a
        href="/auth/signup"
        class="text-sm border border-white lg:px-4 lg:py-2 px-2 py-2 rounded-lg font-semibold hover:bg-white hover:text-black transition-all"
        >Sign Up</a
      >
    </nav>

    <!-- Login Section -->
    <main
      class="flex-1 flex justify-center items-center px-4 sm:px-0 bg-zinc-950"
    >
      <div
        class="bg-zinc-900 border border-gray-700 rounded-2xl shadow-lg p-8 w-full max-w-md"
      >
        <h2 class="text-3xl font-bold text-center mb-6">Welcome Back 👋</h2>
        <form action="/auth/login" method="post" class="flex flex-col gap-5">
          <div>
            <label for="email" class="block mb-1 text-gray-300">Email</label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full px-3 py-2 bg-zinc-800 border border-gray-700 rounded-lg outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="Enter your email"
            />
          </div>

          <div>
            <label for="password" class="block mb-1 text-gray-300"
              >Password</label
            >
            <input
              type="password"
              id="password"
              name="password"
              required
              class="w-full px-3 py-2 bg-zinc-800 border border-gray-700 rounded-lg outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="Enter your password"
            />
          </div>
          <% if(error) { %>
          <div class="text-center text-red-500">
            <p><%= error[0] %></p>
          </div>
          <% } %>

          <button
            type="submit"
            class="bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 rounded-lg transition"
          >
            Login
          </button>
        </form>

        <p class="text-center text-gray-400 text-sm mt-5">
          Don't have an account?
          <a href="/auth/signup" class="text-blue-400 hover:underline"
            >Sign Up</a
          >
        </p>
      </div>
    </main>

    <!-- Footer -->
    <footer
      class="h-14 flex items-center justify-center border-t border-gray-800 text-gray-400 text-sm"
    >
      © <%= new Date().getFullYear() %> NotesApp. All rights reserved.
    </footer>
  </body>
</html>

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign Up | Notes App</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-zinc-950 text-white flex flex-col min-h-screen">
    <!-- Navbar -->
    <nav
      class="h-16 flex justify-between items-center px-10 border-b border-gray-800 bg-zinc-900/60 backdrop-blur-sm"
    >
      <a class="text-2xl font-bold cursor-pointer" href="/">📝 NotesApp</a>

      <a
        href="/auth/login"
        class="border border-white px-4 py-2 rounded-lg font-semibold hover:bg-white hover:text-black transition"
        >Login</a
      >
    </nav>

    <!-- Signup Section -->
    <main
      class="flex-1 flex justify-center items-center px-4 sm:px-0 bg-zinc-950"
    >
      <div
        class="bg-zinc-900 border border-gray-700 rounded-2xl shadow-lg p-8 w-full max-w-md"
      >
        <h2 class="text-3xl font-bold text-center mb-6">
          Create Your Account ✨
        </h2>
        <form action="/auth/signup" method="post" class="flex flex-col gap-5">
          <div>
            <label for="fullName" class="block mb-1 text-gray-300"
              >Full Name</label
            >
            <input
              type="text"
              required
              id="name"
              name="fullName"
              value="<%= oldUserData.length > 0 ? oldUserData[0].fullName : '' %>"
              class="w-full px-3 py-2 bg-zinc-800 border border-gray-700 rounded-lg outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="Enter your name"
            />
          </div>

          <div>
            <label for="email" class="block mb-1 text-gray-300">Email</label>
            <input
              type="email"
              id="email"
              name="email"
              value="<%= oldUserData.length > 0 ? oldUserData[0].email : '' %>"
              required
              class="w-full px-3 py-2 bg-zinc-800 border border-gray-700 rounded-lg outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="Enter your email"
            />
          </div>

          <div>
            <label for="password" class="block mb-1 text-gray-300"
              >Password</label
            >
            <input
              type="password"
              id="password"
              name="password"
              value="<%= oldUserData.length > 0 ? oldUserData[0].password : '' %>"
              required
              class="w-full px-3 py-2 bg-zinc-800 border border-gray-700 rounded-lg outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="Create a password"
            />
          </div>
          <% if (errors) { %>
          <div class="text-center text-red-500">
            <% errors.forEach(err => { %>
            <p><%= err %></p>
            <% }) %>
          </div>
          <% } %>

          <button
            type="submit"
            class="bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 rounded-lg transition"
          >
            Sign Up
          </button>
        </form>

        <p class="text-center text-gray-400 text-sm mt-5">
          Already have an account?
          <a href="/auth/login" class="text-blue-400 hover:underline">Login</a>
        </p>
      </div>
    </main>

    <!-- Footer -->
    <footer
      class="h-14 flex items-center justify-center border-t border-gray-800 text-gray-400 text-sm"
    >
      © <%= new Date().getFullYear() %> NotesApp. All rights reserved.
    </footer>
  </body>
</html>
